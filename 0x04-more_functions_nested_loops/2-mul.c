#include "main.h"

/**
* mul - multiplies two integers
* @a: formal parameter that has integer value passed to it
* @b: fromal parameter that has integer value passed to it
* Return: result of multiplication
*/

int mul(int a, int b)
{
	return (a * b);
}
