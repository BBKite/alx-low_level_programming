#include "main.h"

/**
* _strlen - returns the length of a string
* @s: points to the string outside of the function
* Return: i, the iteration count of the string's characters
*/

int _strlen(char *s)
{
int i;

for (i = 0; i <= s[i]; i++)
	continue;

return (i);
}
