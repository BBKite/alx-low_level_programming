#include "main.h"
/**
* add - returns the result of two added integers
* @x: formal parameter that has value passed to it
* @y: formal parameter that has value passed to it
* Return: added result of x and y
*/
int add(int x, int y)
{
return (x + y);
}
