#include "main.h"
/**
* _abs - computes the absolute value of an integer
* @av: the integer value being computed
*Return: av - the absolute value of integer
*/
int _abs(int av)
{
if (av < 0)
	{
	av *= -1;
	}

return (av);
}
