#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	const char *text = "\"Programming is like building a multilingual puzzle";

	puts(text);

	return (0);
}
